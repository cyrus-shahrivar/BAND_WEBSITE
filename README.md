# Purple Gravy Band Website

## Data Updates

NEWS ITEM EXAMPLE:
```json
"news":[
    {
        "newsPieceHeader": "News Header, can be escaped HTML",
        "newsPieceDetails": [
            "news paragraph, can be escaped HTML",
            "news paragraph, can be escaped HTML",
            "news paragraph, can be escaped HTML"
        ]
    },
    {
        "newsPieceHeader": "News Header, can be escaped HTML",
        "newsPieceDetails": [
            "news paragraph, can be escaped HTML",
            "news paragraph, can be escaped HTML",
            "news paragraph, can be escaped HTML"
        ]
    }
]
```

SHOWS ITEM EXAMPLE:
```json
"shows": [
    {
        "showName": "Shows Header, can be escaped HTML",
        "showDetails": [
            "shows paragraph, can be escaped HTML",
            "shows paragraph, can be escaped HTML",
            "shows paragraph, can be escaped HTML",
        ]
    },
    {
        "showName": "Shows Header, can be escaped HTML",
        "showDetails": [
            "shows paragraph, can be escaped HTML",
            "shows paragraph, can be escaped HTML",
            "shows paragraph, can be escaped HTML",
        ]
    }
]
```